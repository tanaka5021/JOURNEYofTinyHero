using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FieldUp : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        RemainField.FieldNumber = 2;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
